# Портфолио Андрея Брыкуна — Claude Code Context

Последнее обновление: 2026-08-08

## Что это за проект

Личный сайт-портфолио фрилансера (AI, автоматизация, разработка, SEO, безопасность).
Одностраничный статический сайт — один файл `index.html` (~93 KB, инлайновые CSS и JS).

**Владелец:** Андрей Брыкун · Telegram: @Guru_Blockchain

## Где живёт

| | |
|---|---|
| **Локально** | `C:\Projects\portfolio-andrey\` |
| **GitHub** | https://github.com/Andrrreyiv/portfolio-andrey (ветка `main`) |
| **GitHub Pages** | https://andrrreyiv.github.io/portfolio-andrey/ |
| **Vercel** | `.vercel/` — альтернативный деплой |

## Запуск и деплой

```bash
# Локальный предпросмотр (порт 8099 / 8744)
# Через .claude/launch.json → сервер "portfolio-andrey"

# Деплой на продакшн — одна команда:
git push origin main
# GitHub Pages пересобирается ~1 минута
```

## Структура страницы (секции)

```
01 Услуги
02 Примеры работ (кейсы)
03 Как работаю
04 Почему со мной удобно
05 Принципы  ← НОВОЕ (заменило раздел «Отзывы» 2026-06-28)
06 FAQ
   Контакты
```

### Кейсы — важно

- **Кейсы 6–12** — реальные проекты: Швеймаркет, Jetron, Gglass Academy, конструктор футболок,
  Стенариум, AI-ассистент оптовика (обезличен), UX-аудит медцентра (обезличен).
  У пяти из них живые ссылки, два обезличенных идут без ссылок.
- **Кейсы 1–5** — иллюстративные сценарии. Метрики условные. Владелец осознанно оставил «как есть» на 2026-06-28.
- Номера кейсов рисуются CSS-счётчиком (`counter(kase)`), в разметке их нет — при удалении
  или вставке кейса нумерация пересчитывается сама, но **чередование сторон нужно править руками**:
  чётные кейсы несут класс `rev`.

### Обезличенные кейсы — не раскрывать

AI-ассистент оптового поставщика — это ТД «Спаркс». Скриншот в `assets/ai-widget.png` снят
с `widget.sparks.su` и **вручную обезличен** (название и домен заменены на нейтральные через
подмену текстовых узлов в shadow DOM перед съёмкой). Решение владельца от 2026-08-08:
кейс остаётся анонимным, ссылку на демостенд не публиковать — стенд рабочий и содержит
внутренние пометки о ходе проекта.

## История ключевых изменений

### 2026-08-08, вечер — драматический редизайн (коммит `2f82d35`)
- 🔴 **Fraunces не имел кириллицы.** Все русские заголовки сайта с самого начала
  рисовались системной **Georgia**, а не заявленным шрифтом. Проверка: запросить
  `fonts.googleapis.com/css2?family=<Имя>` с полным браузерным User-Agent и
  посмотреть комментарии сабсетов (`/* cyrillic */`). С коротким UA Google отдаёт
  урезанный ответ и кириллица «пропадает» у всех семейств — метод даст ложь.
  Заменён на **Playfair** (не Playfair Display): есть cyrillic + cyrillic-ext,
  оси `opsz 5..1200`, `wdth`, `wght 300..900`.
- **Тёмная тема** на `:root[data-theme="dark"]`. Все полупрозрачные поверхности
  сведены к двум триплетам `--ink-rgb` / `--paper-rgb`, тема меняет их вместо
  правки 17 отдельных `rgba()`. Тема ставится на `<html>` блокирующим скриптом
  в `<head>` — иначе тёмному посетителю мигает белым при каждой загрузке.
- ⚠️ **Зерно надо переключать вместе с темой:** `mix-blend-mode:multiply` поверх
  near-black не рисует ничего. В тёмной — `overlay` и меньше непрозрачность
  (`--grain-blend` / `--grain-op`).
- ⚠️ **Стопка карточек (stacking cards) отвергнута осознанно.** Кейсы сильно
  разной высоты и с большим объёмом текста — стопка обрезала бы содержимое.
  Вместо неё закреплённый (`sticky`) визуал: скриншот стоит, текст едет рядом.
  Для этого `.case` обязан уйти с `align-items:center` на `start`, иначе
  grid-ребёнок не липнет вовсе.
- ⚠️ **`view()` замирает на sticky-элементе.** У прилипшего элемента прямоугольник
  в скроллпорте перестаёт двигаться, и view-таймлайн встаёт посреди анимации.
  Поэтому `view-timeline-name:--case` объявлен на **`.case`** (он не sticky),
  а потребляет его потомок `.plate img`.
- ⚠️ **Магнитная кнопка убивала отклик на нажатие.** JS писал `style.transform`,
  inline-значение, которое `:active` перебить не может — press-feedback не
  работал вообще на устройствах с курсором. Магнит, подъём и нажатие разведены
  по custom properties (`--tx/--ty/--lift/--press`) и складываются в один
  `transform`.
- ⚠️ **Прогресс-бар:** при `animation-timeline:scroll()` обработчик скролла
  обязан перестать писать `style.width`, иначе inline перебивает CSS и полоса
  залипает на 100%. Флаг `progNative`.
- Проверено замерами: оси заголовка идут `wght 330→640`, `opsz 8→120`;
  12/12 визуалов sticky; на мобильном sticky выключен, горизонтальной прокрутки
  нет; при `prefers-reduced-motion` кинетика выключается (`fvs: normal`),
  контент виден, ошибок в консоли нет.

### 2026-08-08 — редизайн «editorial gallery» + моушн-система
- Убран кейс ЭкспоЛид вместе с `assets/expolead.png`.
- Обновлены скриншоты: конструктор футболок (снят с принтом на изделии), AI-виджет
  (обезличенная версия с карточками подбора). Добавлен кейс **Стенариум** со ссылкой на демо.
- Визуальный слой переведён в журнальное направление: крупнее display-шрифт, линейка сверху
  у заголовков секций, номера кейсов, больше воздуха.
- **Убраны размытые цветные пятна фона** (`.bg b` о1/о2/о3) — заменены на бумажный фон
  с колоночной сеткой из волосяных линий. Причина: приём опознаётся как «типовый AI-дизайн»
  и конфликтует с крупной типографикой.
- Моушн-система (архетип Premium): одна кривая `--e-out`, три длительности `--t-q/-s/-l`,
  один паттерн входа. Заголовки набираются по словам, скриншоты раскрываются clip-path'ом
  с осадкой масштаба, у картинок скролл-параллакс ±14px.
- ⚠️ **Глифы проверять в браузере, а не в редакторе:** `Inter` не содержит U+21B3 (`↳`),
  браузер молча подставлял чужой шрифт и в подписи под репликой читалось «Ĭ, ответ на
  основе...». Заменено на `→` (U+2192, в Inter есть) — коммит `61ffd2f`. Правило: любой
  небуквенный символ в вёрстке сверять скриншотом, в исходнике он выглядит правильно всегда.
- ⚠️ **Грабля, стоившая времени:** элемент с `clip-path:inset(0 0 100% 0)` сообщает
  IntersectionObserver нулевую площадь пересечения и **никогда не пересекает порог**, который
  этот clip-path снимает — сам себя блокирует. Поэтому `.plate` наблюдаются **через родителя**
  `.case-visual`, а не напрямую.
- ⚠️ Параллакс пишет только `--py`, а не `transform` целиком: инлайновый `transform` перебил бы
  CSS-осадку масштаба. Масштаб вынесен в `@property --ps`, чтобы анимировать его отдельно —
  иначе drift при скролле тянулся бы через ту же кривую 1.5s и ощущался вязким.
- Добавлен `<noscript>`-стиль: без JS вход-анимации никто не снимет и страница будет пустой.

### 2026-06-28 (коммит `f3eda9c`)
- Убраны 4 выдуманных отзыва с несуществующими людьми и именами.
- Вместо раздела «Отзывы» добавлен раздел **«Принципы работы»** от первого лица:
  - 4 принципа в формате serif-цитат, аватар «АБ»
  - Полноэкранный слайдер (`.tcarousel`, `.ttrack`, `.tslide`, стрелки + dots)
  - Навигация переименована `#reviews → #principles`
- Причина: фейковые отзывы = риск по ФЗ «О рекламе» + репутационный риск.

### Ранее (коммиты `fb7dbde`, `4eabef5`)
- Премиальный редизайн: bento-hero, count-up статистики (`data-count`).
- Магнитные кнопки, cursor-spotlight на карточках `.pcard`/`.gcard`.
- Параллакс фоновых пятен-орбов.
- Reveal-анимации: blur + scale + translateY, с учётом `prefers-reduced-motion`.
- Telegram кнопка → @Guru_Blockchain.

## Технический стек

- Чистый HTML + CSS + JS (без фреймворков, без сборщика).
- Всё инлайново в `index.html` — один файл, нет модулей.
- Анимации: CSS keyframes + IntersectionObserver для reveal.
- Слайдер: чистый JS (touch, keyboard, autoplay, dots, arrows).
- Деплой: GitHub Pages (статика, без CI/CD).

## КРИТИЧЕСКОЕ ПРАВИЛО ЧЕСТНОСТИ

> Сайт презентует **реального человека** реальным клиентам.

**ЗАПРЕЩЕНО добавлять:**
- Выдуманные отзывы с именами или цитатами
- Несуществующих клиентов или их логотипы
- Конкретные метрики, поданные как личный достигнутый результат, если они не верифицированы
- Любой контент, который создаёт ложное впечатление о реальном опыте

**Реальные доказательства** (кейсы 6–12, из них пять с живыми ссылками) — основа доверия. Не трогать без разрешения владельца.

## Начало работы с нуля (другой комп / другой аккаунт)

```bash
git clone https://github.com/Andrrreyiv/portfolio-andrey.git
cd portfolio-andrey

# Предпросмотр локально:
python -m http.server 8099
# → http://localhost:8099

# Деплой после изменений:
git add index.html
git commit -m "Описание изменений"
git push origin main
```

Весь сайт — файл `index.html`. Начинать редактирование с него.
